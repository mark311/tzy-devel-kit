[core]
editor = emacs

[alias]
st = status
br = branch
ci = commit
co = checkout

logx = log -n10 --all --graph --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)%ai%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)'
